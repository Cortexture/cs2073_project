

Int Main { 
Int destinations;

Loop for (destinations--, destinations > 0;) 
{generate int flight (int start point, int destination, int flight time, int seat number) ;
Array[seat types] } 

Fopen (flightDetails.txt)}

Printf(“ Are you an employee?”);

if user is employee {
Int chosenFlight;
List all destinations
Printf(“which flight would you like to pilot”);
scanf(%d, chosenFlight);
}

if user is not employee{
return back to prompt (“Are you an employee?”);
}

Display availableDestinations() function
printf( “choose your destination”):
scanf( %d, destination)

flightDetails(flightTime, seatNum){
Takes user input for desired flight
Print seat array for chosen flight
Lets user choose seats
Check availability (flightTime, seatNum)
Update seat array to block out chosen seats
fprint(flightDetails.txt)
}

if seatNum is available{
confirm reservation (flightDestination,seatNum,flightTime)
printf(“Confirmation message”);
}
else:
Prompt user to re-enter input(“What you have chosen is unavailable, try again”);
Begin through loop again if user chose to re-enter input;
}
Customer side of the program functions:

Variable names:
int destinationChoice, dateChoice, ticketChoice, priceProtectionChoice, quitChoice;

void availableDestinations();
void ticketTypes();
int numTravelers();
void arrivalDates();
void departureDates();


void availableDestinations(){
1). Create a function with no return value since its just a display
2).Print a line stating the following lines are just the destinations that are available
3). Print destination here,newline character.
4). Print destination here,newline character.
5). Print destination here,newline character.
6). Print destination here,newline character.
7). Close function.
**No return
}

void departureDates(){
1). Create a function with no return value since it's just a display
2).Print a line stating the following lines are just the dates that are available
3). Print date here,newline character.
4). Print date here,newline character.
5). Print date here,newline character.
6). Print date here,newline character.
7). Close function.
**No return
}


void arrivalDates(){
1). Create a function with no return value since its just a display
2).Print a line stating the following lines are just the dates that are available
3). Print date here,newline character.
4). Print date here,newline character.
5). Print date here,newline character.
6). Print date here,newline character.
7). Close function.
**No return
}

void ticketTypes(){
1). Create a function with no return value, since its a display.
2). Print a line stating the following is the types of tickets available for purchase.
3). Print ticket type here.
4). Print ticket type here.
5). Print ticket type here.
6). Close function.
**No return
}

int numGuests(){
1). Initialize the variable numTravelers first.
2). Prompt user to enter the number of travelers
3). Store into variable, numTravelers.
4). Return said value (numTravelers)for use later in program wherever applicable.
**Return int value.




